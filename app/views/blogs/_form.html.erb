<%= form_with(model:@blog, local:true, url:choose_new_or_edit )do |f|>
  <div>
    <%= f.label :content %>
    <%= f.text_field :content %>
  </div>
  <div>
    <%= f.label :image %>
    <%= f.file_field :image %>
  </div>
    <%= f.submit "Post" %>
<% end %>
